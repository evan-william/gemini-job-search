description = "Run real job discovery and show the top 50 ranked matches."

prompt = """
# /scrape:50 - Top 50 Discovery Matches

Run the local deterministic command first:

```bash
npm run scrape:show:50
```

Then summarize the strongest patterns from the top 50 ranked matches. Mention
where the full ranked report and raw scrape collection were written.

Do not paste the entire raw JSON into chat.
"""
