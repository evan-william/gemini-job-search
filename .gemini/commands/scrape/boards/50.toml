description = "Run company-board-only scraping and show the top 50 matches."

prompt = """
# /scrape:boards:50 - Company Boards Top 50

Run the local deterministic command first:

```bash
npm run scrape:boards:50
```

Then summarize the strongest patterns from the top 50 matches.
"""
