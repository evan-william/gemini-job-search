description = "Run company-board-only scraping and show the top 25 matches."

prompt = """
# /scrape:boards:25 - Company Boards Top 25

Run the local deterministic command first:

```bash
npm run scrape:boards:25
```

Then summarize the strongest patterns from the top 25 matches.
"""
