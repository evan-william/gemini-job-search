description = "Run company-board-only scraping and show the single best match."

prompt = """
# /scrape:boards:best - Company Boards Top 1

Run the local deterministic command first:

```bash
npm run scrape:boards:best
```

Then summarize the single best match from `jobs/sources.txt`.
"""
