description = "Alias for /scrape:2. Run real discovery and show top 2."

prompt = """
# /scrape:show:2 - Top 2 Discovery Matches

Run:

```bash
npm run scrape:show:2
```

Summarize both matches and mention the output files.
"""
