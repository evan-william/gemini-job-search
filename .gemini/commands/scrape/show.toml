description = "Run real job discovery and show the default top 10 ranked matches."

prompt = """
# /scrape:show - Default Top 10 Discovery Matches

Run the local deterministic command first:

```bash
npm run scrape:show
```

Then summarize the top ranked matches from the terminal output. Mention where
the full ranked report and raw scrape collection were written.

Do not re-rank manually unless the local command fails.
"""
